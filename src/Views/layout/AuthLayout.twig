<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>{{ title }}</title>
		<link rel="stylesheet" href="/make-up/src/Public/Css/Auth.css">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"/>
	</head>
	<body class="AuthLayout">
		<header class="site-header">
			<div class="menuAuth">
				<span class="title">
					{{ type }}
				</span>
				<nav>
					<a href='/make-up/' class="backShop">
						<span class='material-symbols-outlined'>
							arrow_back_ios
						</span>
						Back to Shop
					</a>
					{# {!user?.roles?.[0] && ( #}
					<a href="register" class=''>
						<span class="material-symbols-outlined">
							app_registration
						</span>
						Register
					</a>
					{# )} #}
					{# {!user?.roles?.[0] && ( #}
					<a href="login" class=''>
						<span class='material-symbols-outlined'>
							how_to_reg
						</span>
						Login
					</a>
					{# )} #}
				</nav>
			</div>
		</header>
		{% block main %}
			<div class="center">
				{% block content %}{% endblock %}
			</div>
		{% endblock %}
	</body>
</html>
