<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>{{ title }}</title>
		<link rel="stylesheet" href="/make-up/src/Public/Css/Public.css">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"/>

	</head>
	<body class="PublicLayout">
		<div class='HeaderShop'>
			<div class='top'>
				<span>100% original products!</span>
				<ul>
					<li>
						<a href=''>Actions</a>
					</li>
					<li>
						<a href=''>MAKEUP Club</a>
					</li>
					<li>
						<a href=''>Delivery and Payment</a>
					</li>
					<li>
						<a to=''>Articles</a>
					</li>
					<li>
						<a href=''>About the store</a>
					</li>
				</ul>
				<span>Office</span>
			</div>
			<div class='bottom'>
				<div class='connection'>
					<a href=''>0(800) 50 77 40</a>
					<p>Every day from 7:55 to 20:05</p>
					<button>Call back</button>
				</div>
				<a href={publicUrl}>
					<img src='/make-up/src/Public/Img/logo.svg'/>
				</a>
				<div class='user'>
					<div
						class='userAuth'>
						{# {!isAuth && ( #}
						<a href={publicUrl + authUrl + TypeAuth.LOGIN}>
							<span class='material-symbols-outlined'>person</span>
						</a>
						{# )} #}
						{# {isAuth && ( #}
						<div
							class='authBlock'>
							{# <span>{user.username}</span> #}
							<span class='userOut'>
								<span class='material-symbols-outlined'>logout</span>
							</span>
						</div>
						{# )} #}
					</div>
					{# {basket.basketModal && #}
					<div><BasketModal/></div>

					{# } #}
					<div class='basket'>
						<span class="material-symbols-outlined">
							shopping_bag
						</span>
					</div>
					<div>
						<SearchHeaderStore/>
					</div>
				</div>
			</div>
		</div>
	</body>
</html></div></div>{% block content %}<div class='mainContent'>
{{ block('main') }}</div>{% endblock %}</body></html>
